using System;

namespace GitProgram
{
    class UserService
    {
        public void CreateUser(string name)
        {
            Console.WriteLine("User created: " + name);
        }
    }
}
